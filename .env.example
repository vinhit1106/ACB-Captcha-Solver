# Optional server-side credentials. Prefer sending credentials in each request
# when deploying this public-facing API.
ACB_USERNAME=
ACB_PASSWORD=
# Optional authenticated browser session. Never commit the real values.
ACB_COOKIE_HEADER=
ACB_DSE_SESSION_ID=
MAX_CAPTCHA_RETRIES=2
REQUEST_TIMEOUT_SECONDS=30
